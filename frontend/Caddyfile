:{$PORT}

root * /app/dist
encode gzip

route {
  # /api/* -> backend (quitando /api del path)
  handle_path /api/* {
    reverse_proxy https://confident-grace-production-331f.up.railway.app
  }

  # SPA
  try_files {path} /index.html
  file_server
}
